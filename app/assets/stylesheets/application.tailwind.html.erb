<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    table {
      background-color: #faf4f0; /* Màu  */
    }
    .product {
      color: #5e310c; /* Màu chữ của phần tử product */
    }
    .supplier {
      color: #5e310c; /* Màu chữ của phần tử order */
    }
    .center1 {
      text-align: center;
      font-size: 100px;
      color: #5e310c;
      font-weight: bold;
    }

    .center1:hover {
      transform: translate(2px, -2px); /* làm cho chữ chuyển động khi rê chuột vào */
      text-shadow: 4px 4px 6px rgba(0, 0, 0, 0.5); /* tăng độ mờ của bóng đổ khi rê chuột vào */
      transition: all 0.2s ease-in-out; /* thêm hiệu ứng transition cho chữ */
    }
    
    img { /* căn giữa */
      display: block;
      margin-left: auto;
      margin-right: auto;
    }
    
    .center2 {
      text-align: center;
      font-size: 20px; /* phóng to kích thước chữ */
      color: #5e310c; /* thay đổi màu sắc chữ */
    }

    .center2:hover {
      transform: translate(2px, -2px); /* làm cho chữ chuyển động khi rê chuột vào */
      text-shadow: 4px 4px 6px rgba(0, 0, 0, 0.5); /* tăng độ mờ của bóng đổ khi rê chuột vào */
      transition: all 0.2s ease-in-out; /* thêm hiệu ứng transition cho chữ */
    }
  </style>
  <title>Your HTML Page</title>
</head>
<body>
  <!-- Your HTML content goes here -->
</body>
</html>
